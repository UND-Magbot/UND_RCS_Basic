# Noah 일일 업무 보고 2026.05.20

## 1. RCS Basic 모델 개발

### 다중 로봇 조율
- POI 락 시스템 (`poi_lock.py`) — 같은 POI 동시 배차 차단
- 데드락 자동 감지 + 양보 (`deadlock_monitor.py`) — `/planning_state` + `/nearby_robots` 폴링, `is_waiting_for_dest`/`remaining_distance` 정체 25초 기준
- 양보 후 30초 뒤 자동 재시도 (`yield_robot_job` + `_paused_route_jobs`)
- 마주침 사전 양보 — peer 근접 시 임계 15초로 단축
- Zone 락 시스템 (`zone_lock.py` + `zone_guard.py` + `geometry.py`) — 좁은 통로를 polygon으로 정의해 한 번에 한 로봇만 통과
- 단일 로봇 운영 시 데드락 감지 자동 skip

### 작업 안전망
- `align_with_retry` 4단계 회복 — 재로컬화 → 후진 0.5m → 측면 우회 0.3m
- `safe_move` — 통신 실패/이동 실패/장애물 정체 자동 재시도 (최대 200회 ≈ 17분)
- 충전소 도킹 사전 접근 지점 적용 (yaw 반대 60cm) — 충전기 충돌 방지
- `wait_for_confirm` 기본 timeout 30분 추가 (좀비 작업 방지)
- `_running_robots` Lock + `_window` thread-safety + 좀비 전역 dict 정리

### 로봇 제어 — 일시정지/재개/강제 종료
- `pause_robot_job` / `resume_robot_job` — 현재 이동 cancel + 재개 시 자동 재시도
- `force_return_and_dock` — 잭업 → 랙 위치 보관 → 충전소 도킹 일괄 수행
- 모니터링 작업 카드에 ⏸ 정지 / ▶ 재개 / ■ 강제 종료 / 즉시 중단 버튼

### 수동 배차 다중 어사인먼트
- `POST /api/tasks/manual-run-batch` — 로봇별 (작업 종류, 작업1·2 위치, 위치 간 대기, 반복 횟수) 명세 + 무한 반복 지원
- `BatchDispatchPanel` 신규 UI + 단일/배치 탭 전환

### POI/맵 시스템
- `MapPOI.rack_size` 컬럼 추가 (S600/S300) — DB 마이그레이션 안내
- POI 편집 팝업: "대기 지점 → 랙 위치", "작업 포인트 → 작업 위치"로 라벨 변경, S300/S600 사이즈 선택 (랙 위치 전용)
- 랙 사이즈별 마커 크기 분기 (S300 = 0.73×0.74m, S600 = 0.83×0.87m)
- POI 자동 이름 생성 (C1/J1/R1/W1 — 타입별 prefix + max+1)
- POI 타입 변경 시 자동 이름 갱신
- 충전소 마커 표시 위치 보정 (로봇 후방 0.5m)

### 맵 관리 흐름 개선
- 맵 페이지 ↔ 모니터링 페이지 사업장·영역 공유 (localStorage `rcs:selectedBusiness`, `rcs:selectedArea`)
- 맵 관리 "적용" 버튼 + `POST /api/map/default-area/{area_id}` — 현재 영역을 모니터링 메인 디폴트로 등록 (JSON 파일 영구 저장)
- 맵 저장 시 같은 area의 다른 맵 POI를 이름 매칭으로 자동 재매핑 (`remap_task_waypoints_to_map`)
- 맵 변경 후 최신 맵 자동 선택 버그 수정 (`data.items[0]`)
- 매핑 완료 후 그 영역 자동 선택

### UI 정리
- MapToolbarLeft 제거 → MapToolbarTop 한 줄 통합 (되돌리기 / 포인트 / 작업 포인트 / 가상벽 / 삭제 / 충전소 / 현 위치)
- 적용 버튼 저장 앞 배치, 다른 버튼과 스타일 통일
- 모니터링 POI 락(빨간 링) + Zone 락(우측 알림 패널) 시각화

### S300 rack.specs 정확화
- 실제 감지값(0.73 × 0.74)과 일치하도록 spec 조정
- rack.specs 순서: S300 우선 배치 (펌웨어 매칭 대응)
- 4곳 일괄 적용 (sync, sync-overlays, 마커 시각화, POI 편집 라벨)

### rack_pickup 작업 흐름
- 충전소 → 랙 위치(빈 랙 픽업) → 작업 위치(짐 적재/하역) → 랙 위치(랙 보관) → 충전소
- standby(W1) → 랙 위치 명칭 통일 (`_return_rack_to_standby`)

### 기타
- `robot_type` 타입 통합 (tsc 에러 해결)
- daemon thread 공통 예외 wrapper (`thread_utils.safe_thread`)
- API 디버깅: `GET /api/tasks/poi-locks`, `GET /api/tasks/zone-locks`, `GET /api/tasks/deadlock-monitor`, `POST /api/tasks/routes/remap-to-map/{id}`

## 2. DB 마이그레이션 필요
```sql
ALTER TABLE map_pois ADD COLUMN rack_size VARCHAR(10) NULL;
```

## 3. 미해결
- 12006 Wrong rack size — 펌웨어가 `rack.specs[0]`만 매칭하는지 확인 필요. spec 순서 변경 후 동기화 + 재시작 적용해 검증 중

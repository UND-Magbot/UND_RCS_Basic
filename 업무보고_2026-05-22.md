# Noah 일일 업무 보고 2026.05.22

## 1. RCS Basic 모델 개발

### 강제 종료 — work_mode 별 동작 분기
- `force_return_and_dock` 이 모든 작업을 동일하게 "잭업 → 랙 위치 복귀 → 잭다운 → 충전소" 로 처리하던 문제 정리
- `_active_route_jobs` 메타에서 work_mode 를 `stop_robot_job` 호출 전에 선캡쳐 (작업 스레드가 finally 에서 pop 하기 전)
- 분기 동작
  - `rack_pickup` : 현재 위치 잭업 → 랙 위치(standby POI) 로 to_unload_point → 잭다운 → 충전소 (기존 동작)
  - `delivery_no_rack` / `simple_move` : 잭/랙 단계 모두 스킵, 곧장 `_return_to_charger`
  - 메타 없음(잭 테스트 등) : `rack_pickup` 폴백 — 잭이 들려 있을 가능성을 가정해 안전 디폴트
- `activity_log` 에 work_mode 같이 기록 → 사후 추적 가능

### job-status API 에 work_mode 노출
- `run_route_job` 진입 시 `update_job_status(ip, work_mode=work_mode)` 1회 호출
- `GET /api/robots/job-status/{ip}` 응답에 work_mode 포함 → 프론트가 작업 종류 인지
- 폴링 주기마다 갱신 없이 한 번만 머지 (성능 영향 없음)

### 모니터링 화면 종료 동작 일원화
- 사용자 정리: "강제종료/즉시종료 중 즉시종료는 불필요 — 강제종료 + 일시정지만 있으면 된다"
- `ActiveJobsPanel.tsx` (좌측 작업 카드)
  - **"즉시 중단" 버튼 제거** + 미사용 `handleStop` 정리
  - `RobotJob` 타입에 `work_mode?: string` 추가, 폴링에서 함께 수신
  - `handleForceReturn` 의 confirm 문구를 work_mode 별 동적 분기
    - `rack_pickup` → "랙을 원래 위치에 두고 충전소로 복귀합니다"
    - 그 외 → "현재 작업을 중단하고 곧바로 충전소로 복귀합니다"
  - 상단 주석에서 "즉시 중단" 항목 삭제, work_mode 분기 설명 추가
- `JobStatusPanel.tsx` (우측 작업 현황)
  - `handleStopAndDock` (`stop-all` 호출) → `handleForceReturn` (`force-return` 호출) 로 교체
  - 버튼 라벨 **"정지" → "강제 종료"**, work_mode 별 confirm 문구 동일하게 적용
  - `JobInfo` 인터페이스에 `work_mode?: string` 추가
- 결과: 모니터링 두 패널 모두 **⏸ 정지(pause) / ▶ 재개 / ■ 강제 종료** 로 통일

### stop-all API 보존
- 백엔드 `/api/robots/remote/stop-all/{ip}` 라우터는 그대로 유지
- 다음 경로는 "그 자리에 멈춤" 의도가 살아있어야 함
  - `JackTestPanel` — 잭 테스트 도중 정지
  - `RemoteControlModal` — 원격 수동 조종 컨텍스트
  - `tablet.html` — 태블릿 앱 빠른 정지

## 2. 진단 / 디버깅 작업
- `stop-all` 호출처 5곳(태블릿 2 + 모니터링 3 + JackTest + RemoteControl) 식별해 영향 범위 분류
- ActiveJobsPanel / JobStatusPanel 의 정지 버튼이 사용자에게 같은 동작으로 보이는 원인 추적 — 둘 다 stop-all 호출이라 충전소 복귀 동선 차이가 없었음
- `_active_route_jobs` 메타 lifecycle 확인 (run_route_job 진입 시 set, finally 에서 pop) — force_return_and_dock 도 finally 에서 pop 하므로 캡쳐는 함수 진입 직후가 안전

## 3. 미해결
- 잭 테스트(`run_jack_job`) 흐름은 `_active_route_jobs` 에 등록되지 않아 force_return 호출 시 항상 rack_pickup 분기로 동작. 잭 테스트 중 강제종료가 호출되는 시나리오 자체가 드물고, 랙을 들고 있을 가능성이 가장 안전한 가정이라 폴백으로 충분하다고 판단. 별도 흐름 필요 시 후속.

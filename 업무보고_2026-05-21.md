# Noah 일일 업무 보고 2026.05.21

## 1. RCS Basic 모델 개발

### rack.specs 매칭 문제 해결 (12006 Wrong rack size)
- 펌웨어가 여러 spec 등록 시 잘못된 spec(S600)과 비교해 실패하던 문제 추적
- 신규 모듈 `constants/rack_specs.py` — `RACK_SPECS` dict + 사이즈별 정의
- 최종 방식: **로봇 모델명 기준 자동 매핑**으로 spec 1개만 전송
  - `crawler_s300_op5` → S300 (0.765 × 0.765)
  - `crawler_heavy` → S600 (0.83 × 0.87)
- sync / sync-overlays 두 곳에서 `build_rack_specs_for_robot_model(model)` 사용
- 로그에 `model=…, spec=…` 출력해 검증 가능
- S300 spec 값을 s300 정상 버전과 동일(0.765)으로 통일

### rack_pickup 작업 흐름 — B 패턴 (잭업/잭다운 무한 반복)
- `run_route_job` 에 `start_jacked` / `end_jacked` 옵션 추가
- 드롭오프마다 jack_down → 대기 → align + jack_up (마지막이고 `end_jacked=False` 만 jack_down 으로 끝)
- 배치 `_run` 단순화: `is_first` / `is_last_iteration` 로 standby pickup·return 자동 제어
  - 1회차: standby pickup ✓, end_jacked=True
  - 중간 회차: 잭업 상태로 시작/종료
  - 마지막 회차: jack_down 후 standby 복귀 → 충전소 도킹
- 무한 반복은 `is_last_iteration` 항상 False — 잭업 유지하며 작업1 ↔ 작업2 무한 반복

### pause / resume 흐름 견고화
- `wait_move` 가 paused 중 cancel 감지 시 결과에 `_paused_cancel: True` 표시
- `safe_move` / `align_with_retry` 모두 `_paused_cancel` 보면 같은 이동 자동 재시도
  - 이전: resume 후 cancelled 받아 `_fail()` → 작업 사라짐
  - 지금: 같은 attempt 그대로 재실행 (재시도 카운트 미차감)

### 데드락 모니터 점진적 완화 → 비활성화
- 1단계: `aligning` / `jacking_up` / `jacking_down` / `charging` / `returning` / `waiting*` 상태 감지 제외
- 2단계: `remaining_distance < 1.0m` (정밀 도착 단계) 감지 제외
- 3단계: **전체 비활성화** — 사이트에 좁은 통로 없어 양보 불필요
  - `main.py` 에서 `deadlock_monitor.start/stop` 주석 처리 + import 비활성
  - POI 락 / Zone 락 / safe_move 자동 재시도 / align_with_retry 회복은 그대로 작동

### 충전소 도킹 안정화
- `_return_to_charger` 사전 접근(yaw 반대 60cm) 단계 — `failed to calc global path` 같은 무한 재시도 회피
  - safe_move → `create_move` + `wait_move` 1회만 (best-effort), 실패해도 즉시 charge 단계로 진행
- `run_route_job` 내 charging waypoint 처리도 동일하게 변경

### POI 자동 이름 생성 & 타입 전환 갱신
- 타입별 prefix max+1: 충전소 **C**, 작업 위치 **J**, 랙 위치 **R**, 경유지 **W**
- POIEditPopup 에서 타입 변경 시 콜백(`getNextNameForType`)으로 새 이름 자동 갱신
- 사용자가 input 으로 자유 수정 가능

### UI 통합·정리
- `ActiveJobsPanel.tsx` 신규 — 작업 중 로봇 카드 + 제어(⏸ 정지 / ▶ 재개 / ■ 강제 종료 / 즉시 중단 / 출발 / 복귀 / 다음 포인트)
  - `DispatchTabs` 상단에 공통 표시 (단일/배치 어느 탭이든 보임)
  - 배치 실행 후 폼 리셋돼도 제어 버튼 그대로 노출
- `BatchDispatchPanel` 레이아웃 1열 stack 으로 — 좁은 패널에서 글자 안 잘림
  - 라벨 단순화: "작업 1 위치" / "작업 2 위치" (픽업/드롭오프 표기 제거)
  - 위치 간 대기·반복 횟수만 2열, 헤더 강조, "+ 작업 추가" primary 컬러
- `MapToolbarTop` 한 줄 통합 — 되돌리기 / 포인트 / 작업 포인트 / 가상벽 / 삭제 / 충전소 / 현 위치
- `MapToolbarLeft` 더 이상 사용 안 함
- "적용" 버튼 저장 앞으로 이동 + 다른 버튼과 동일 스타일

### 맵 ↔ 모니터링 동기화
- `GET /api/map/default-area` 응답에 `business_id` 포함 — 맵 페이지가 정확한 사업장 자동 선택
- 맵 페이지 진입 시 default-area 우선 사용 → 메인 적용한 영역이 바로 표시
- `GET /api/map/active-pois` → area 의 **모든 활성 맵 POI 통합** 반환 (이전엔 최신 맵 1개만)
  - 새로 추가한 충전소/랙 위치가 다른 맵에 있으면 안 보이던 버그 수정

### 위치재조정 — 충전기 도킹 상태 기준
- 우선순위 `standby_id` → `charging_id` 로 변경
- 충전소 좌표 + `target_yaw = yaw + π` (로봇 정면이 충전기 반대 방향, 즉 빠져나오는 방향)
- 도킹 직후 정확한 자세로 펌웨어에 알림

### 충전소 시각화 보정
- 모니터링 충전소 마커를 도킹 좌표에서 angle 반대 0.5m 이동시켜 표시
- 로봇 도킹 시 마커가 본체에 가려지지 않도록

## 2. 진단 / 디버깅 작업
- `192.168.0.27/60/62` 로봇 직접 폴링해 rack.specs 적용 확인 (모두 spec 1개로 0.765 정확 반영)
- standby_id / charging_id 비교로 락 충돌 가능성 배제 (모두 다름)
- 활동로그 + 작업이력 cross-check 으로 실패 패턴 분석 (rack_detection_error → deadlock_yield → cancel)
- POI 좌표 비교로 R1/R2 vs R3 차이 분석 (시스템상 차이 없음 → 물리적 환경/POI 등록 정확도 원인)

## 3. 미해결
- align_with_rack 실패 시(`rack_detection_error`) — `align_with_retry` 의 4단계 회복(재로컬화 / 후진 / 측면 우회)으로도 인식 못하는 케이스. POI 좌표가 실제 랙 위치와 어긋났을 가능성. 운영자가 "현 위치" 버튼으로 재등록 필요할 수 있음.
- C4/R4 처럼 area 내 다른 맵에 분산된 POI 들의 통합 관리 — `active-pois` 는 통합으로 변경됨. 다만 운영 측에서 같은 area 의 맵 정리 필요할 수 있음.
